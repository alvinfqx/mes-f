/*
* bootstrap-table - v1.9.0 - 2015-09-30
* https://github.com/wenzhixin/bootstrap-table
*/
!function (a) {
    "use strict";

    var data = [
        {
            formatLoadingMessage: function () { return "Loading, please wait..." },
            formatRecordsPerPage: function (a) { return c("%s records per page", a) },
            formatShowingRows: function (a, b, d) { return c("Showing %s to %s of %s rows", a, b, d) },
            formatSearch: function () { return "Search" },
            formatNoMatches: function () { return "No matching records found" },
            formatPaginationSwitch: function () { return "Hide/Show pagination" },
            formatRefresh: function () { return "Refresh" },
            formatToggle: function () { return "Toggle" },
            formatColumns: function () { return "Columns" }
        },
        {
            formatLoadingMessage: function () { return "正在努力地加载数据中，请稍候……" },
            formatRecordsPerPage: function (a) { return "每页显示 " + a + " 条记录" },
            formatShowingRows: function (a, b, c) { return "显示第 " + a + " 到第 " + b + " 条记录，总共 " + c + " 条记录" },
            formatSearch: function () { return "搜索" },
            formatNoMatches: function () { return "没有找到匹配的记录" },
            formatPaginationSwitch: function () { return "隐藏/显示分页" },
            formatRefresh: function () { return "刷新" },
            formatToggle: function () { return "切换" },
            formatColumns: function () { return "列" }
        },
        {
            formatLoadingMessage: function () { return "正在努力地加載數據中，請稍候……" },
            formatRecordsPerPage: function (a) { return "每頁顯示 " + a + " 條記錄" },
            formatShowingRows: function (a, b, c) { return "顯示第 " + a + " 到第 " + b + " 條記錄，總共 " + c + " 條記錄" },
            formatSearch: function () { return "搜索" },
            formatNoMatches: function () { return "沒有找到匹配的記錄" },
            formatPaginationSwitch: function () { return "隱藏/顯示分頁" },
            formatRefresh: function () { return "刷新" },
            formatToggle: function () { return "切換" },
            formatColumns: function () { return "列" }
        }
    ];

    var index = 0;

    if (language) {
        switch (language) {
            case "en":
                index = 0;
                break;
            case "zh-cn":
                index = 1;
                break;
            case "zh-tw":
                index = 2;
                break;
        }
    }

    a.fn.bootstrapTable.locales["zh-cn"] = data[index], a.extend(a.fn.bootstrapTable.defaults, a.fn.bootstrapTable.locales["zh-cn"])
}(jQuery);

 
